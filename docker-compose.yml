# version: '3.8'
services:
  backend:
    container_name: nft-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    env_file:
      - ./backend/.env
    depends_on:
      - mongo
      # - blockchain
    volumes:
      # - ./backend:/app
      - photos-volume:/app/photos
    restart: always
    networks:
      - nft-network

  mongo:
    image: mongo:latest
    container_name: nft-mongo
    command: mongod --replSet rs0 --bind_ip_all
    # ports:
    #   - '27017:27017'
    volumes:
      - mongo-data:/data/db
    networks:
      - nft-network

volumes:
  mongo-data:
    driver: local
  photos-volume:
    driver: local

networks:
  nft-network:
    driver: bridge
